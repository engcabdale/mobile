<?php
@include 'config.php';

if(isset($_GET['name'])) {
    $name = $_GET['name'];
    // Query to update data with the provided name
    // Execute the query
    // Redirect back to admin page
    }
    ?>
